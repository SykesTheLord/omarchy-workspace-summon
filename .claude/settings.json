{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PROJECT_DIR:-.}/.claude/hooks/plugin-check.sh\"",
            "timeout": 20,
            "statusMessage": "Checking plugin manifest and QML..."
          }
        ]
      }
    ]
  }
}
